import styled from 'styled-components';
import {
  space,
  width,
  height,
  display,
  maxWidth,
  alignItems,
  borderLeft,
  borderColor,
  borderBottom,
  flexDirection,
  justifyContent
} from 'styled-system';

export const Section = styled.section`
  ${space}
  ${width}
  ${height}
  ${display}
  ${maxWidth}
  ${alignItems}
  ${borderLeft}
  ${borderBottom}
  ${borderColor}
  ${flexDirection}
  ${justifyContent}
`;
